// Shared config — used by both app and dashboard
// Replace with your Supabase project values
export const SUPABASE_URL = 'https://YOUR_PROJECT.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_ANON_KEY';

// How close (meters) the player must be to collect an egg
export const PROXIMITY_RADIUS_M = 15;

// Game ID — hardcoded for v1, could be dynamic later
export const GAME_ID = 'YOUR_GAME_UUID';
